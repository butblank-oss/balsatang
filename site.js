/* 사업자정보 — 한 줄에 모아 둔다.

   data.js 와 같은 방식이다. 선언이 한 줄이라 어드민이 그 줄만 갈아끼워
   커밋할 수 있다. 값을 여러 파일에 흩어 놓으면 상호가 바뀔 때 한 곳이 남는다.

   ── 빈 값은 화면에서 그 줄을 통째로 뺀다 ──
   '준비 중' 같은 문구를 대신 넣지 않는다. 없는 걸 있는 것처럼 보이게 하는 쪽이
   비어 있는 것보다 나쁘다. 사업자등록번호가 없는데 자리만 있으면 사용자는
   '곧 생기나 보다' 로 읽는다 — 그건 우리가 하지 않기로 한 종류의 말이다. */
const SITE={"name":"","owner":"","bizNo":"","mailOrderNo":"","address":"","email":"","updatedAt":"2026-08-04"};
